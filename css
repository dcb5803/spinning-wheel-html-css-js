.box {
  width: 300px;
  height: 300px;
  border-radius: 50%;
  border: 10px solid #fff;
  position: relative;
  transition: transform 5s ease-out; /* The 'ease-out' makes it stop slowly */
}
/* Style individual slices with absolute positioning and rotation */
.box span {
  position: absolute;
  width: 50%;
  height: 50%;
  transform-origin: bottom right;
}
